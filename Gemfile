source 'https://rubygems.org'

  # Cucumber-Rails installation
  group :test do
	  #gem 'cucumber-rails', :require => false
	  # database_cleaner is not required, but highly recommended
	  #gem 'database_cleaner'
    # add rspec for assertion usage
    gem 'rspec-expectations'
    gem 'capybara'
    gem 'cucumber'
    gem 'rspec'
    gem 'selenium-webdriver'
    gem 'phantomjs'
    # gem 'phantomjs', :git => "git://github.com/jovistruck/phantomjs-gem.git" #to do: Alternative repo for where the gem has a npm download link for phantomjs than the unreliable bitbucket link as was in the original
    gem 'poltergeist'
    gem 'pry'
    gem 'capybara-screenshot'
    gem 'site_prism'
    gem 'chromedriver-helper'
  end
